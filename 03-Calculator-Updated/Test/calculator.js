// Using Functions to avoid reuse of code;

// initialize a Variable calculation to an empty string;
let calculation = "";

function updateCalculator(value) {
    calculation += value;
    console.log(calculation);
}